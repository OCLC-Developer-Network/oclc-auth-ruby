language: ruby

before_install:
  - rvm get head
  - gem install bundler

rvm:
  - 2.2
  - jruby

# configure notifications (email, IRC, campfire etc)
notifications:
    email:
        recipients:
        - coombsk@oclc.org
        on_success: always
        on_failure: always